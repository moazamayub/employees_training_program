<div class="min-h-screen bg-slate-50 py-8 px-4 sm:px-6 lg:px-8">
  <div class="max-w-2xl mx-auto">
    <div class="mb-6">
      <%= link_to "← Back to Companies", admin_companies_path, class: "text-xs font-semibold text-blue-600 hover:text-blue-800 uppercase tracking-wider" %>
      <h1 class="text-2xl font-bold text-blue-950 mt-2">Add New Company</h1>
      <p class="text-sm text-slate-500">Register a new client company into the training platform.</p>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6 sm:p-8">
      <%= render "form", company: @company %>
    </div>
  </div>
</div>